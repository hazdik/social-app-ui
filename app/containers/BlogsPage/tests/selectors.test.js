// import { fromJS } from 'immutable';
// import { makeSelectBlogsPageDomain } from '../selectors';

// const selector = makeSelectBlogsPageDomain();

describe('makeSelectBlogsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
