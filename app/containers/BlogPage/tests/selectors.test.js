// import { fromJS } from 'immutable';
// import { makeSelectBlogPageDomain } from '../selectors';

// const selector = makeSelectBlogPageDomain();

describe('makeSelectBlogPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
