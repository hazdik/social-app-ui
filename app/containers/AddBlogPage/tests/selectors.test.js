// import { fromJS } from 'immutable';
// import { makeSelectAddBlogPageDomain } from '../selectors';

// const selector = makeSelectAddBlogPageDomain();

describe('makeSelectAddBlogPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
