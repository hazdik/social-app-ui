// import { fromJS } from 'immutable';
// import { makeSelectMediaLibraryPageDomain } from '../selectors';

// const selector = makeSelectMediaLibraryPageDomain();

describe('makeSelectMediaLibraryPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
