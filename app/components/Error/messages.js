/*
 * Error Messages
 *
 * This contains all the text for the Error component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  retry: {
    id: 'app.components.error.retry',
    defaultMessage: 'Retry',
  },
});
